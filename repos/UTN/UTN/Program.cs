using System;

//Solucion, namespace
namespace UTN
{
    //Clase PROGRAM
    internal class Program
    {
        //Dentro de la clase, esta el metodo MAIN, es lo primero que se ejecuta
        static void Main(string[] args)
        {
            //Notas. 
            /*
            ===========================================================================================================
            ===========================================================================================================

            CLASE 1

            descuento = 0.87M; //'M' por decimal. 'F' por float y 'L' por long. (literales).

            Al ingresar un float o numero con coma en la consola, usar ',' y no '.'

            variableMax = int/float/etc.MaxValue(); devuelve el maximo que se puede almacenar de un tipo;
            variableMin = int/float/etc.MinValue(); devuelve el minimo que se puede almacenar de un tipo;

            Los nombres de las variables deben ser nombres especificos para orientar a quien lo lea, sin necesidad de buscar a que se refiere.

            ===========================================================================================================
            ===========================================================================================================
           
            CLASE 2
             
            Por cada clase, un archivo individual dentro del proyecto en el que se trabaja.
            # Clic derecho >> Add >> Class >> Class >> name and add.


            ===========================================================================================================
            ===========================================================================================================
            CLASE 3 - Bilioteca de clases

            Metodos estaticos: no se necesita instancias la clase u objeto al que esta asociado


            */
        }
    }
}
